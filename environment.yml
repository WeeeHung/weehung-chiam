# Conda environment specification for Atlantis
name: agent-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      # Core dependencies
      - google-genai>=0.2.0
      - python-dotenv>=1.0.0
      - requests>=2.31.0
      # FastAPI backend
      - fastapi>=0.104.0
      - uvicorn[standard]>=0.24.0
      - pydantic>=2.5.0
